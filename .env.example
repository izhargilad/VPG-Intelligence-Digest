# VPG Intelligence Digest - Environment Variables
# Copy this file to .env and fill in your values

# ============================================================
# Anthropic API (for AI analysis and scoring)
# ============================================================
ANTHROPIC_API_KEY=your-anthropic-api-key-here
ANTHROPIC_MODEL=claude-sonnet-4-5-20250929
ANTHROPIC_TEMPERATURE=0.3
ANTHROPIC_MAX_TOKENS=4096

# ============================================================
# Gmail API (for email delivery)
# ============================================================
# Two ways to provide OAuth2 credentials (pick one):
#
# OPTION A (recommended): Paste the full credentials JSON here.
#   This avoids committing secrets to Git.
#   Copy the entire contents of your downloaded credentials.json:
# GMAIL_CREDENTIALS_JSON={"installed":{"client_id":"...","project_id":"...","auth_uri":"...","token_uri":"...","auth_provider_x509_cert_url":"...","client_secret":"...","redirect_uris":["http://localhost"]}}
#
# OPTION B: Place the credentials.json file directly in config/
#   (config/credentials.json is gitignored)
#
# Then run: python -m src.delivery.auth
# This creates config/token.json (auto-refreshes)
#
# Sender address:
GMAIL_SENDER_EMAIL=vpg.intelligence@gmail.com

# ============================================================
# Delivery Mode
# ============================================================
# 'mock'  — write digests as local HTML files (for preview/testing)
# 'gmail' — send via Gmail API (requires authorization, see above)
DELIVERY_MODE=mock

# Output directory for mock mode HTML digests
MOCK_OUTPUT_DIR=./data/mock-digests

# ============================================================
# Application Settings
# ============================================================
# Database path
DATABASE_PATH=./data/vpg_intelligence.db

# Logging
LOG_LEVEL=INFO
LOG_DIR=./logs

# Config UI
CONFIG_UI_PORT=3000
API_PORT=8000

# ============================================================
# Scraping Settings
# ============================================================
# User agent for HTTP requests
USER_AGENT=VPG-Intelligence-Agent/1.0
# Delay between requests (ms) - be respectful of rate limits
REQUEST_DELAY_MS=2000
# Maximum concurrent scrapers
MAX_CONCURRENT_SCRAPERS=3
