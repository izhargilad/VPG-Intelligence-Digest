# VPG Intelligence Digest - Environment Variables
# Copy this file to .env and fill in your values

# ============================================================
# Anthropic API (for AI analysis and scoring)
# ============================================================
ANTHROPIC_API_KEY=your-anthropic-api-key-here
ANTHROPIC_MODEL=claude-sonnet-4-5-20250929
ANTHROPIC_TEMPERATURE=0.3
ANTHROPIC_MAX_TOKENS=4096

# ============================================================
# Gmail Email Delivery
# ============================================================
#
# RECOMMENDED: SMTP with App Password (simplest — no OAuth, no files)
#   1. Enable 2-Step Verification: https://myaccount.google.com/security
#   2. Generate App Password: https://myaccount.google.com/apppasswords
#      → Select "Mail" → Copy the 16-character password
#   3. Set these two values and DELIVERY_MODE=smtp below
#
GMAIL_SENDER_EMAIL=your-email@gmail.com
GMAIL_APP_PASSWORD=xxxx xxxx xxxx xxxx
#
# ALTERNATIVE: Gmail API with OAuth2 (for advanced use)
#   Place credentials.json in config/ and run: python -m src.delivery.auth
#   Then set DELIVERY_MODE=gmail
#
# GMAIL_CREDENTIALS_JSON=  # Optional: paste full JSON instead of file

# ============================================================
# Delivery Mode
# ============================================================
# 'mock'  — write digests as local HTML files (for preview/testing)
# 'smtp'  — send via Gmail SMTP + App Password (recommended)
# 'gmail' — send via Gmail API + OAuth2
DELIVERY_MODE=smtp

# Output directory for mock mode HTML digests
MOCK_OUTPUT_DIR=./data/mock-digests

# ============================================================
# Application Settings
# ============================================================
# Database path
DATABASE_PATH=./data/vpg_intelligence.db

# Logging
LOG_LEVEL=INFO
LOG_DIR=./logs

# Config UI
CONFIG_UI_PORT=3000
API_PORT=8000

# ============================================================
# Scraping Settings
# ============================================================
# User agent for HTTP requests
USER_AGENT=VPG-Intelligence-Agent/1.0
# Delay between requests (ms) - be respectful of rate limits
REQUEST_DELAY_MS=2000
# Maximum concurrent scrapers
MAX_CONCURRENT_SCRAPERS=3
